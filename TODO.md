# TODO — tilemap-studio

**Status markers:** `[ ]` open · `[wip]` in progress · `[verify]` implemented, verification
not yet run+recorded · `[x]` done (`## Done` only). The bracket also carries a delegation
tier, tier last — `[T4]`, `[wip T2]`, `[verify T3]`. See `~/CLAUDE.md` — Delegation.

## Open

_Nothing open._

## Watch

_Nothing being watched._

## Parked

### Feature wishlist (upstream)

Unowned, unscheduled ideas carried over verbatim from this file's original
`## Features` list — no plan, no date, no commitment. Deliberately Parked rather
than Open: an unowned wishlist sitting in Open would misreport the backlog and
invite `/next` to dispatch it. Promote an entry to Open, and rank it then, when
it is actually being picked up.

- **Palettes:** Combine .pal files with .\*bpp files for accurate tileset rendering
- **.tileset files:** Read and export lists of images with start+offset+length values
- Support .4bpp.lz files with GBA LZ compression
- Native-looking build on Mac OS X (involves publishing an app bundle release, and using the system menu bar)
- Scale the UI for high-DPI displays
- Generate tilemap images from the command line
- Allow undo/redo for resize operations

## Done

_Nothing recorded yet._
